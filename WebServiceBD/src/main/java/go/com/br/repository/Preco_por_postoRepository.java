package go.com.br.repository;

import go.com.br.model.Preco_por_posto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Preco_por_postoRepository extends JpaRepository<Preco_por_posto, Long> {
}
